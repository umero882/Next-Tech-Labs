{
  "name": "nexttech-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Next Tech Labs — portfolio site. Static, web-only, no backend.",
  "scripts": {
    "dev": "vite",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "prebuild": "node scripts/generate-sitemap.mjs && node scripts/generate-blog-feed.mjs",
    "build": "vite build",
    "postbuild": "node scripts/prerender.mjs",
    "prerender": "node scripts/prerender.mjs",
    "preview": "vite preview --host",
    "test": "node --test",
    "verify": "node scripts/verify-routes.mjs",
    "feed": "node scripts/generate-blog-feed.mjs"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest",
    "framer-motion": "latest",
    "lucide-react": "latest",
    "react-window": "latest",
    "use-debounce": "latest",
    "clsx": "latest",
    "tailwind-merge": "latest"
  },
  "devDependencies": {
    "vite": "latest",
    "@vitejs/plugin-react": "latest",
    "tailwindcss": "latest",
    "@tailwindcss/vite": "latest"
  },
  "engines": {
    "node": ">=20.19"
  }
}
